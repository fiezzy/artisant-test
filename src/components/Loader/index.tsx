import { VFC } from "react";
import { Wrapper, Label } from "./style";

const Loader: VFC = () => {
  return (
    <Wrapper>
      <Label>Loading..</Label>
    </Wrapper>
  );
};

export default Loader;
